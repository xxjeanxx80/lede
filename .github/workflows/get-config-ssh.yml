name: Get LEDE Config Fixed for RAX3000Q

on:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'Bật SSH qua tmate'
        required: false
        default: 'true'

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Kiểm tra mã nguồn
      uses: actions/checkout@v4

    - name: Cài đặt môi trường
      run: |
        sudo apt update
        sudo apt install -y ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential \
        bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib \
        git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libglib2.0-dev libgpgme11-dev libltdl-dev \
        libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libreadline-dev libssl-dev libtool lrzsz \
        mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf python3 python3-pip libpython3-dev \
        qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget \
        xmlto xxd zlib1g-dev

    - name: Tải mã nguồn LEDE (Cập nhật sâu)
      run: |
        # Clone với độ sâu đầy đủ để không sót các branch/patch
        git clone https://github.com/coolsnowwolf/lede
        cd lede
        
        # Cập nhật mã nguồn và các gói phụ trợ
        ./scripts/feeds update -a
        ./scripts/feeds install -a
        
        # Kiểm tra sự hiện diện của dòng chip Qualcomm mới
        echo "--- DANH SÁCH TARGET IPQ HIỆN CÓ ---"
        ls -d target/linux/ipq* || echo "Không tìm thấy thư mục ipq nào"
        echo "------------------------------------"

    - name: Mở kết nối SSH (tmate)
      uses: mxschmitt/action-tmate@v3
      if: github.event.inputs.ssh == 'true'
      # Đã loại bỏ limit-access-to-actor để tránh lỗi thiếu SSH Key trên profile GitHub

    - name: Kết thúc
      run: echo "Xong."
